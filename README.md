# dev-environment

Version-controlled Linux development environment for WSL.

## Bootstrap

This repository is designed to bootstrap a fresh Ubuntu WSL environment.

From a fresh WSL installation:

```bash
git clone git@github-personal:VictorAut/dev-environment.git ~/personal/dev-environment
cd ~/personal/dev-environment
./bootstrap.sh